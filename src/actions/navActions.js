import { NAV_CUSTOMER, NAV_MAIN } from './types';

export const navCustomer = () => {
    return {
        type: NAV_CUSTOMER
    }
}

export const navMain = () => {
    return {
        type: NAV_MAIN
    }
}